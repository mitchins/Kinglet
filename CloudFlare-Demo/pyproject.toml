[project]
name = "kinglet-worker"
version = "0.1.0"
description = "Kinglet on Cloudflare Workers"
requires-python = ">=3.12"
dependencies = [
    "kinglet @ git+https://github.com/mitchins/Kinglet.git",
]

[dependency-groups]
dev = ["workers-py"]
